FROM python:3.9-slim

WORKDIR /app

# Copy necessary files
COPY . .

# Install dependencies if any, add more commands if necessary
# RUN pip install some-dependency

# Default command (adjust if needed)
CMD ["bash"]
